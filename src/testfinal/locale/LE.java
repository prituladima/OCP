package testfinal.locale;

import java.util.Locale;

public class LE {
    public static void main(String[] args) {
        Locale locale = Locale.getDefault();
        System.out.println(locale);
    }
}
